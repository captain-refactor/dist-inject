{
    "name": "dist-inject",
    "version": "0.0.1",
    "description": "",
    "main": "src",
    "types": "src",
    "scripts": {
        "test": "mocha --require ts-node/register ./src/**/*.spec.ts",
        "build": "tsc",
        "coverage:test": "nyc mocha ./src/**/*.spec.js",
        "coverage:report": "nyc report --reporter=json && codecov -f coverage/*.json"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {},
    "devDependencies": {
        "@types/mocha": "^5.2.5",
        "@types/node": "^10.7.1",
        "codecov": "^3.1.0",
        "mocha": "^5.2.0",
        "nyc": "^13.0.1",
        "reflect-metadata": "^0.1.12",
        "ts-node": "^7.0.1",
        "typescript": "^3.0.3"
    }
}
