{
    "name": "dist-inject",
    "version": "0.0.1",
    "description": "",
    "main": "src",
    "types": "src",
    "scripts": {
        "test": "mocha --require ts-node/register ./src/**/*.spec.ts",
        "build:docs": "typedoc --out ./docs",
        "add:docs": "git add ./docs",
        "build": "tsc"
    },
    "husky": {
        "hooks": {
            "pre-commit": "npm run build:docs && npm run add:docs"
        }
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {},
    "devDependencies": {
        "@types/mocha": "^5.2.5",
        "@types/node": "^10.7.1",
        "husky": "^1.0.1",
        "mocha": "^5.2.0",
        "reflect-metadata": "^0.1.12",
        "ts-node": "^7.0.1",
        "typescript": "^3.0.3"
    }
}
